# لیست کارهای سئو — سمت سرور (Server-Side)

بر اساس مواردی که سئوکار مشخص کرده، این لیست فقط **مواردی که باید سمت سرور (یا با تغییر معماری رندر) انجام شوند** را پوشش می‌دهد.

---

## مواردی که باید سمت سرور فیکس شوند (خلاصه)

### لیست

1. **صفحهٔ محصول:** نام محصول، عکس و قیمت و تگ `<h1>` در سورس خام (View Source) دیده نمی‌شوند — باید حداقل بخش بالای صفحه با Server Component یا هیبرید سمت سرور رندر شود.
2. **صفحهٔ محصولات:** Canonical و og:url باید مطلق باشند (مثل `https://microless.ir/products/`) و اسلش انتهایی داشته باشند؛ الان relative و بدون اسلش هستند.
3. **صفحهٔ محصولات:** اسکیمای BreadcrumbList برای مسیر صفحه (مثلاً خانه > محصولات) سمت سرور اضافه شود.
4. **صفحهٔ محصولات:** اسکیمای ItemList برای لیست محصولات همین صفحه سمت سرور اضافه شود.
5. **صفحهٔ محصولات (و در صورت نیاز هوم):** یک تگ `<h1>` مناسب (مثلاً «محصولات») اضافه شود؛ ترجیحاً سمت سرور.

### جدول

| # | مورد | صفحه | اقدام | فایل / مکان |
|---|------|------|--------|-------------|
| 1 | نام/عکس/قیمت و `<h1>` در سورس خام | محصول | رندر بخش بالای صفحه با Server Component یا هیبرید | `app/product/[productId]/page.js` |
| 2 | Canonical و og:url مطلق + اسلش انتهایی | محصولات | تبدیل url به مطلق و اضافه کردن اسلش انتهایی | `utils/metadata.js` (createMetadata)، `app/products/layout.js` |
| 3 | اسکیمای BreadcrumbList | محصولات | تزریق JSON-LD در layout یا کامپوننت سرور | `app/products/layout.js` یا کامپوننت سرور جدید |
| 4 | اسکیمای ItemList (لیست محصولات) | محصولات | ساخت اسکیما از دادهٔ لیست محصولات در رندر سرور | صفحهٔ محصولات با fetch اولیه در سرور |
| 5 | تگ `<h1>` | محصولات (و در صورت نیاز هوم) | اضافه کردن `<h1>` معنادار؛ ترجیحاً در سرور | `ProductsClient` / `HeaderSection` یا کامپوننت سرور صفحهٔ محصولات |

---

## ۱. صفحهٔ محصول (`/product/[productId]`)

### ۱.۱ متاتگ‌ها و Canonical (در حال حاضر سمت سرور هستند ✅)
- **وضعیت:** در `layout.js` با `generateMetadata` و `fetchProductForMeta` انجام شده است.
- **Title، Description، Open Graph، Twitter، Canonical** با دیتای واقعی محصول پر می‌شوند.
- **Canonical:** با اسلش انتهایی ساخته می‌شود: `${base}/product/${productId}/`
- **تأیید:** اگر دامنهٔ نهایی `https://microless.ir` است، `NEXT_PUBLIC_SITE_URL` را در env به همان مقدار (با یا بدون اسلش انتهایی) تنظیم کنید تا canonical و og:url همیشه مطلق و یکسان باشند.

### ۱.۲ اسکیما JSON-LD (در حال حاضر سمت سرور هستند ✅)
- **وضعیت:** در `ProductSchemaScript.js` (کامپوننت سرور) اسکیمای **Product** و **BreadcrumbList** تزریق می‌شوند و در View Source دیده می‌شوند.

### ۱.۳ محتوای بدنهٔ صفحه (نام، عکس، قیمت) در سورس خام دیده نمی‌شود ⚠️
- **علت:** کامپوننت صفحهٔ محصول (`page.js`) **"use client"** است و دیتا با `useEffect` + API در کلاینت لود می‌شود. بنابراین در اولین پاسخ سرور فقط اسکلتون/خالی رندر می‌شود و بعد از اجرای جاوااسکریپت محتوا نمایش داده می‌شود؛ در **View Source** نام محصول، عکس و قیمت داخل HTML نیست.
- **کار پیشنهادی سئو:** تبدیل به **Server Component** (یا هیبرید: بخش بالای صفحه مثل `<h1>` و خلاصهٔ محصول سمت سرور با همان `fetchProductForMeta` رندر شود) تا تگ `<h1>` حاوی نام محصول و در صورت امکان تصویر و قیمت، **مستقیماً در سورس خام** دیده شوند.
- **نکته:** اگر به‌خاطر دمو/اسکرپر فعلاً کل صفحه Client است، بعد از تثبیت دیتای محصول از API، می‌توان یک لایهٔ سرور برای همان بخش اول (هدر + h1 + یک بلوک خلاصه) اضافه کرد.

### ۱.۴ BAILOUT_TO_CLIENT_SIDE_RENDERING
- **علت:** به‌خاطر وجود `"use client"` در `page.js`، کل درخت آن صفحه در سمت کلاینت رندر می‌شود؛ بنابراین Next/React آن را به عنوان رندر کلاینت علامت می‌زند و محتوای واقعی در HTML اولیه قرار نمی‌گیرد.
- **راه‌حل:** با تبدیل بخش محتوای ثابت (حداقل h1 و خلاصه) به Server Component یا با پیش‌بارگذاری دیتا در سرور و پاس دادن به کلاینت، می‌توان از رندر اولیهٔ معنادار در سورس اطمینان حاصل کرد.

---

## ۲. صفحهٔ دسته‌بندی / لیست محصولات (`/products`)

### ۲.۱ Canonical و og:url — مطلق و با اسلش انتهایی ❌
- **وضعیت فعلی:** در `products/layout.js` از `createMetadata` با `url: "/products"` استفاده شده است. در `utils/metadata.js` تابع `createMetadata` مقدار `url` را بدون تبدیل به آدرس مطلق در `alternates.canonical` و `openGraph.url` قرار می‌دهد؛ بنابراین خروجی **نسبت (relative)** می‌شود و **اسلش انتهایی** هم اعمال نمی‌شود.
- **حالت درست (طبق سئو):**
  - `canonical`: `https://microless.ir/products/` (مطلق + اسلش انتهایی)
  - `og:url`: همان آدرس مطلق با اسلش انتهایی
- **کار سمت سرور:**
  1. در `createMetadata` همیشه برای `url`های مسیر (مثل `/products`) یک **آدرس مطلق** بسازید (با `NEXT_PUBLIC_SITE_URL` یا معادل) و برای مسیرهایی مثل `/products` **اسلش انتهایی** اضافه کنید.
  2. در `products/layout.js` یا همان جایی که metadata صدا زده می‌شود، یا `url` را به صورت مطلق + اسلش انتهایی پاس دهید، یا در `createMetadata` هر `url` که با `/` شروع می‌شود را به `baseUrl + url` (و در صورت نبود اسلش انتهایی، اضافه کردن آن) تبدیل کنید.

### ۲.۲ اسکیما برای صفحهٔ دسته‌بندی ❌
- **BreadcrumbList:** برای مسیر صفحه (مثلاً خانه > محصولات یا خانه > دسته‌بندی) در **سرور** اسکیمای BreadcrumbList تولید و در `<head>` یا ابتدای بدنه تزریق شود.
- **ItemList:** برای **لیست محصولات** نمایش‌داده‌شده در همین صفحه، اسکیمای **ItemList** (با آیتم‌های محصول) در سرور ساخته و به صورت JSON-LD در همان صفحه قرار گیرد. این کار نیاز به در دست داشتن لیست محصولات در زمان رندر سرور دارد (مثلاً با `generateMetadata` یا یک کامپوننت سرور که دادهٔ اولیهٔ لیست را fetch می‌کند).

### ۲.۳ تگ `<h1>` در صفحهٔ محصولات ❌
- **وضعیت:** در `ProductsClient` و `HeaderSection` هیچ تگ `<h1>` وجود ندارد (مثل هوم‌پیج).
- **کار سمت سرور/ساختار:** اضافه کردن یک `<h1>` مناسب برای صفحهٔ محصولات (مثلاً «محصولات» یا عنوان دسته/جستجو). اگر بخش بالای این صفحه در یک Server Component رندر شود، می‌توان همان `<h1>` را آنجا قرار داد تا در سورس خام دیده شود؛ در غیر این صورت حداقل در کلاینت یک `<h1>` معنادار اضافه شود (ترجیحاً در سرور برای سئو بهتر است).

---

## ۳. خلاصهٔ چک‌لیست سمت سرور

| # | مورد | صفحه | وضعیت | اقدام |
|---|------|------|--------|--------|
| 1 | متاتگ Title, Description, OG با دیتای واقعی | محصول | ✅ انجام شده | فقط تأیید خروجی و دامنه |
| 2 | Canonical با اسلش انتهایی | محصول | ✅ انجام شده | فقط تأیید `NEXT_PUBLIC_SITE_URL` |
| 3 | اسکیمای Product + BreadcrumbList | محصول | ✅ انجام شده | — |
| 4 | نام/عکس/قیمت و `<h1>` در سورس خام | محصول | ❌ | تبدیل بخش محتوا به Server Component یا هیبرید سرور/کلاینت |
| 5 | Canonical و og:url مطلق + اسلش انتهایی | محصولات | ❌ | اصلاح `createMetadata` و `products/layout.js` |
| 6 | اسکیمای BreadcrumbList | محصولات | ❌ | اضافه در layout/کامپوننت سرور صفحهٔ محصولات |
| 7 | اسکیمای ItemList (لیست محصولات) | محصولات | ❌ | اضافه با دادهٔ سرور در همان صفحه |
| 8 | تگ `<h1>` | محصولات (و در صورت نیاز هوم) | ❌ | اضافهٔ `<h1>` مناسب در صفحهٔ محصولات (ترجیحاً سمت سرور) |

---

## ۴. فایل‌های مرتبط برای تغییر

- **متا و canonical محصولات:** `amazon-shop/src/utils/metadata.js` (تابع `createMetadata`)، `amazon-shop/src/app/products/layout.js`
- **صفحهٔ محصول (رندر اولیه):** `amazon-shop/src/app/product/[productId]/page.js` (تبدیل به Server یا هیبرید)
- **اسکیما و h1 صفحهٔ محصولات:** `amazon-shop/src/app/products/layout.js`، `amazon-shop/src/app/products/page.js`، یا یک کامپوننت سرور جدید برای هدر + اسکیما
- **دامنهٔ نهایی:** `.env` یا `.env.local` — `NEXT_PUBLIC_SITE_URL=https://microless.ir` (یا دامنهٔ واقعی با/بدون اسلش؛ در کد اسلش انتهایی برای مسیرها جداگانه اضافه شود)

اگر بخواهید می‌توانم قدم‌به‌قدم برای هر مورد (مثلاً اصلاح `createMetadata` یا اضافه کردن اسکیمای ItemList) کد پیشنهادی بدهم.
