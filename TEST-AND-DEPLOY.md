# بررسی جریان و تست (سرچ، ذخیره، خرید)

## ✅ چک‌لیست جریان (همه فیکس و آماده است)

| مرحله | وضعیت | توضیح |
|--------|--------|--------|
| **جستجو** | ✅ | فرانت با `NEXT_PUBLIC_SCRAPER_URL` مستقیماً به پایتون `GET /api/search?q=...` می‌زند. |
| **نمایش نتایج** | ✅ | `ProductsClient` با `searchQuery` از URL، `searchAmazon()` را صدا می‌زند و لیست را نرمال و در کارت‌ها نمایش می‌دهد. |
| **کلیک روی محصول** | ✅ | `ProductCard` با همان آبجکت اسکرپ، `saveIfNotExistsFromScraper` را به بک‌اند می‌زند و بعد به `/product/{productId}` ریدایرکت می‌کند. |
| **ذخیره در DB** | ✅ | بک‌اند `POST Product/SaveIfNotExists` با ASIN چک و در صورت نبود ذخیره می‌کند. |
| **CORS** | ✅ | پایتون: `cors_origins = "*"`. بک‌اند .NET: `AllowAnyOrigin()`. درخواست از لوکال‌هاست هم قبول می‌شود. |

---

## تست لوکال (روی سیستم خودت)

می‌توانی **بدون گذاشتن روی سرور** تست کنی، به یکی از این دو شکل:

### حالت ۱: فرانت لوکال، اسکرپر و بک‌اند روی سرور

- روی سیستم خودت در پوشه فرانت: `npm run dev`
- در مرورگر برو به `http://localhost:3000`
- جستجو بزن (مثلاً از هدر به `/products?search=lenovo` برو)

**شرط:** از شبکه/اینترنت به آدرس سرور (مثلاً `107.161.175.45`) دسترسی داشته باشی.  
درخواست‌ها از **مرورگر** به `107.161.175.45:5000` (اسکرپر) و `107.161.175.45:8080` (بک‌اند) می‌روند. با CORS فعلی پایتون و .NET مشکلی نیست.

فایل **`.env.local`** همین‌طور با آدرس سرور باشد (همان که الان داری):

```env
NEXT_PUBLIC_API_URL=http://107.161.175.45/api
NEXT_PUBLIC_SCRAPER_URL=http://107.161.175.45:5000
```

### حالت ۲: هر سه سرویس روی یک ماشین (همان سرور یا همان ویندوز)

اگر اسکرپر و بک‌اند را روی **همان ماشین** (مثلاً سرور یا کامپیوتر خودت) اجرا می‌کنی:

- اسکرپر: مثلاً `http://localhost:5000`
- بک‌اند: مثلاً `http://localhost:8080/api`

در **`.env.local`** می‌توانی موقتاً بگذاری:

```env
NEXT_PUBLIC_API_URL=http://localhost:8080/api
NEXT_PUBLIC_SCRAPER_URL=http://localhost:5000
```

بعد `npm run dev` و تست جستجو و کلیک روی محصول.

---

## تست روی سرور (هر سه روی یک سرور)

- فرانت، بک‌اند و اسکرپر را روی همان سرور بالا بیاور.
- در فرانت از **`.env`** یا env داکر همان آدرس سرور استفاده می‌شود (مثلاً `107.161.175.45` یا دامنه).
- در مرورگر به آدرس سایت برو و جستجو و کلیک محصول را تست کن.

برای داکر، همان **`.env`** کنار `docker-compose` کافی است و مقدار پیش‌فرض `NEXT_PUBLIC_SCRAPER_URL` هم در compose تنظیم شده است.

---

## جمع‌بندی

- جریان سرچ و ذخیره و لینک به صفحه محصول **فیکس و کامل** است.
- هم می‌توانی **لوکال** (با فرانت روی سیستم خودت و سرویس‌ها روی سرور یا لوکال) تست کنی، هم **روی سرور** بعد از دیپلوی.
