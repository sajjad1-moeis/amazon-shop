# متن برای ارسال به همکار سئو

---

سلام،

بر اساس چک‌لیست و نکاتی که برای سئو فرستادی، وضعیت سمت سرور رو خلاصه می‌کنم تا تصویر روشنی داشته باشی:

---

**۱. صفحهٔ محصول (مثلاً /product/21/)**

- **متاتگ‌ها:** تگ‌های Title، Description و Open Graph (و توییتر) الان سمت سرور با دیتای واقعی همان محصول پر می‌شن. منبع دیتا همون API محصوله و در زمان رندر سرور فراخوانی می‌شه.
- **Canonical:** تگ canonical هم سمت سرور ست می‌شه و قانون اسلش انتهایی رعایت شده؛ یعنی خروجی به صورت `https://دامنه/product/21/` هست. اگر دامنهٔ نهایی مثلاً microless.ir باشه، با تنظیم متغیر env سایت (NEXT_PUBLIC_SITE_URL) همین آدرس مطلق و یکسان در همه‌جا استفاده می‌شه.
- **اسکیما (JSON-LD):** هم اسکیمای Product و هم BreadcrumbList سمت سرور تزریق می‌شن و در View Source قابل مشاهده‌ان.

**مشکلی که گفتی — نام محصول، عکس و قیمت توی سورس خام نیست:**  
الان کل صفحهٔ محصول به صورت Client-Side رندر می‌شه (به‌خاطر نیاز به تعامل و دمو/اسکرپر). یعنی در اولین پاسخ سرور فقط قالب خالی یا اسکلتون ارسال می‌شه و بعد از لود جاوااسکریپت، دیتا از API گرفته می‌شه و نام، عکس و قیمت نمایش داده می‌شن. به همین دلیل در View Source این محتواها داخل HTML دیده نمی‌شن.  
پیشنهاد فنی ما اینه که بعد از تثبیت دیتای محصول، حداقل بخش بالای صفحه (مثل همان تگ h1 و در صورت امکان خلاصهٔ محصول) رو سمت سرور رندر کنیم تا در سورس خام هم نام محصول و در حد امکان تصویر و قیمت برای گوگل موجود باشه. همین موضوع باعث می‌شه اون BAILOUT_TO_CLIENT_SIDE_RENDERING که دیدی برای همون بخش برطرف بشه.

---

**۲. صفحهٔ دسته‌بندی / لیست محصولات (/products)**

این بخش چند تا نقطه داره که سمت سرور باید اصلاح یا اضافه بشه:

- **Canonical و og:url:** الان آدرس canonical و og:url برای این صفحه به صورت relative (مثل /products) و بدون اسلش انتهایی ست می‌شن. طبق استانداردی که گفتی باید مطلق باشن و اسلش انتهایی داشته باشن؛ مثلاً `https://microless.ir/products/`. این رو در تابعی که متادیتا رو می‌سازه اصلاح می‌کنیم تا همیشه برای چنین مسیرهایی خروجی مطلق و با اسلش انتهایی باشه.
- **اسکیما:** برای صفحهٔ محصولات قراره سمت سرور اسکیمای BreadcrumbList (مسیر صفحه، مثلاً خانه > محصولات) و اسکیمای ItemList (لیست محصولات همین صفحه) رو اضافه کنیم تا در View Source در دسترس باشن.
- **تگ h1:** الان در صفحهٔ محصولات (و مشابهش در هوم) هیچ تگ h1 ای وجود نداره. قراره یک h1 مناسب (مثلاً «محصولات» یا عنوان دسته/جستجو) اضافه بشه؛ ترجیحاً سمت سرور تا در سورس خام هم دیده بشه.

---

**خلاصه برای تو:**

- **صفحهٔ محصول:** متاتگ‌ها، canonical با اسلش انتهایی و اسکیماهای Product و BreadcrumbList الان سمت سرور درست پیاده‌ان. تنها مورد باقی‌مانده اینه که نام/عکس/قیمت و h1 در سورس خام دیده بشن؛ که با رندر سمت سرور حداقل برای بخش بالای صفحه انجام می‌دیم.
- **صفحهٔ محصولات:** canonical و og:url مطلق با اسلش انتهایی، اسکیمای BreadcrumbList و ItemList و یک h1 مناسب رو سمت سرور اضافه/اصلاح می‌کنیم.

اگر جایی برات مهمه که ترتیب یا اولویت این کارها مشخص باشه یا بخوای بعد از هر مرحله خروجی رو چک کنیم، بگو تا هماهنگ کنیم.

---
