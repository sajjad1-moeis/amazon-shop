# نگاشت داده اسکرپر/بک‌اند به نمایش در فرانت

هر فیلدی که اسکرپر یا بک‌اند برای محصول برمی‌گرداند، در صورت وجود در همین سمت **داینامیک** نمایش داده می‌شود.

---

## ۱. تصاویر (چند عکس)

فرانت از **همه** این فیلدها استفاده می‌کند (اولین آرایهٔ غیرخالی):

| منبع (اسکرپر/بک‌اند) | نحوهٔ استفاده |
|----------------------|----------------|
| `product.images`     | آرایهٔ URLها → گالری صفحهٔ محصول |
| `product.imageUrls` | همان |
| `product.image_urls`| همان |
| `product.gallery`   | همان |
| در غیر این صورت     | از `image_url_hq` و `image_url` یک یا دو تصویر ساخته می‌شود |

- اگر اسکرپر فقط `image_url` و `image_url_hq` بفرستد و هر دو یکی باشند، فقط **یک** عکس نمایش داده می‌شود.
- برای **چند عکس**: اسکرپر/بک‌اند باید یکی از فیلدهای آرایه‌ای بالا (مثلاً `images` یا `image_urls`) را با لیست URLها پر کند.
- در کلیک از نتایج جستجو، اگر محصول `images` یا `image_urls` داشته باشد در sessionStorage و در payload ذخیره می‌شود و بعد از ذخیره در DB با پاسخ `getById` ادغام می‌شود تا همان تصاویر در صفحهٔ محصول نشان داده شوند.

---

## ۲. قیمت و ارز

| فیلد | نمایش |
|------|--------|
| `discountPrice` / `current_price` / `price` | قیمت نهایی (همان «قیمت») |
| `original_price` / `price` | قیمت قبل از تخفیف (خط خورده) |
| `currency` یا `currency_symbol === "AED"` | واحد «درهم»؛ وگرنه «تومان» |

تخفیف درصدی از اختلاف همین دو قیمت محاسبه و نمایش داده می‌شود.

---

## ۳. فروشگاه و ارسال

| فیلد | نمایش |
|------|--------|
| `amazonShopName` یا `seller` | متن کنار لوگو (مثلاً «آمازون امارات»)؛ در صورت نبود: «آمازون امارات» |

بدون این فیلدها مقدار ثابت «آمازون امارات» نشان داده می‌شود.

---

## ۴. امتیاز و تعداد نظرات

| فیلد | نمایش |
|------|--------|
| `rating` | امتیاز (مثلاً ۴.۲) |
| `reviews_count` یا `reviewCount` | تعداد نظرات |

هر دو در باکس امتیاز و در کنار عنوان محصول استفاده می‌شوند.

---

## ۵. لیست نظرات (کاربر به کاربر)

اگر اسکرپر/بک‌اند آرایهٔ نظرات بفرستد، **همه** در بخش «امتیاز و نظرات کاربران دیگر» نمایش داده می‌شوند.

| فیلد در هر نظر | نام‌های پشتیبانی‌شده |
|----------------|----------------------|
| نام کاربر      | `name`, `author`, `reviewerName` |
| متن نظر       | `comment`, `text`, `body`, `content` |
| امتیاز        | `rating`, `stars` |
| تاریخ          | `date`, `createdAt` |
| لایک/دیس لایک | `likes`, `dislikes` (اختیاری) |

- اگر `product.reviews` آرایه و طولش > 0 باشد، کارت‌های نظر از روی همین آرایه رندر می‌شوند.
- اگر فقط `rating` و `reviews_count` بیاید و `reviews` نیاید، فقط امتیاز و تعداد و متن «این امتیاز و تعداد نظر از آمازون است» نشان داده می‌شود.

---

## ۶. بقیهٔ فیلدهای محصول

| فیلد | استفاده در فرانت |
|------|-------------------|
| `title` / `name` | عنوان محصول |
| `brand` / `brandName` | برند |
| `description` / `shortDescription` | دربارهٔ محصول (آکاردیون و معرفی کوتاه) |
| `attributes` (آرایهٔ `{ name, value }`) | مشخصات فنی / ویژگی‌ها |
| `category` / `categoryName` | مسیر breadcrumb و دسته‌بندی |

---

## جمع‌بندی

- **قیمت، تخفیف، درهم، فروشگاه، امتیاز، تعداد نظر، توضیحات، مشخصات فنی**: همگی از همان آبجکت محصولی که اسکرپر/بک‌اند برمی‌گردانند داینامیک هستند.
- **چند عکس**: با پر کردن یکی از فیلدهای آرایه‌ای `images` / `image_urls` / `imageUrls` / `gallery` در خروجی اسکرپر یا در پاسخ API محصول.
- **لیست نظرات**: با پر کردن `product.reviews` (آرایهٔ اشیاء با فیلدهای بالا).

اگر جایی هنوز متن ثابت می‌بینید، یعنی آن فیلد در پاسخ API/اسکرپر برای آن محصول ارسال نشده است؛ با اضافه کردن همان فیلد در خروجی اسکرپر یا بک‌اند، در همین UI بدون تغییر فرانت نمایش داده می‌شود.
